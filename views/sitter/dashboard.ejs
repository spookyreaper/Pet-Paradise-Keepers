<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <title>Sitter Dashboard</title>
</head>
<body>
  <%- include('../partials/header') %> 

  <div class="dashboard-wrapper">
    <aside class="sidebar">
      <h2>Welcome, <%= user.firstName %> <%= user.lastName %></h2>
      <!-- Sitters might not have a wallet or promo codes, adjust or remove these -->
      <button class="button" onclick="location.href='/sitter/profile';">Update Profile</button>
      <button class="button" onclick="location.href='/sitter/appointments';">View Appointments</button>
      <button class="button" onclick="location.href='/sitter/availability';">Set Availability</button>
      <button class="button" onclick="logout();">Logout</button>
    </aside>
    
    <main class="main-content">
      <h1>Your Sitter Dashboard</h1>
      <div class="referral-info">
        <h2>REFER A FRIEND, GET $20</h2>
        <p>Invite other sitters or pet owners and earn credits when they join and complete a booking.</p>
        <button class="logout-button">Invite a Friend</button>
      </div>

      <!-- Adjust or replace this section according to what sitters should see -->
      <div class="appointment-info">
        <h2>Your Upcoming Appointments</h2>
        <!-- Dynamically list upcoming appointments. Placeholder content here. -->
        <p>No upcoming appointments. Get ready to help more pets!</p>
      </div>
      
      <!-- Sitters likely don't need to add pets, so this section is commented out -->
      <!-- <button onclick="location.href='/add-pet';" class="add-pet-button">Add Pet</button> -->
    </main>
  </div>

  <%- include('../partials/footer') %> 

  <script>
    function logout() {
      fetch('/logout', { method: 'POST' })
        .then(response => {
          if (response.ok) {
            window.location.href = '/login';
          } else {
            throw new Error('Logout failed.');
          }
        })
        .catch(error => console.error('Error:', error));
    }
  </script>  
</body>
</html>
