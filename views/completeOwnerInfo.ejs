<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <title>Complete Your Profile</title>
</head>
<style>
    body {
  font-family: 'Arial', sans-serif;
  line-height: 1.6;
  margin: 0;
  padding: 0;
  background: #e9ecef;
}

.container {
  max-width: 500px;
  margin: 30px auto;
  padding: 20px;
  background: #ffffff;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

h1 {
  color: #333;
  text-align: center;
  margin-bottom: 30px;
}

/* Style for form elements */
form {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

label {
  font-weight: 600;
  color: #495057;
}

input[type="text"],
input[type="file"],
textarea,
button {
  padding: 10px;
  border: 1px solid #ced4da;
  border-radius: 0.25rem;
  transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
}

input[type="text"]:focus,
input[type="file"]:focus,
textarea:focus {
  border-color: #80bdff;
  outline: 0;
  box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
}

textarea {
  resize: vertical;
}

button {
  font-size: 1rem;
  color: #fff;
  background-color: #007bff;
  border: none;
  padding: 10px;
  border-radius: 5px;
  cursor: pointer;
  box-shadow: 0 2px 2px 0 rgba(0, 0, 0, 0.05);
  transition: all 0.2s ease-in-out;
}

button:hover, button:focus {
  background-color: #0056b3;
  box-shadow: 0 4px 4px 0 rgba(0, 0, 0, 0.1);
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .container {
    padding: 15px;
  }
}

</style>
<body>

  <%- include('partials/header') %> 

  <div class="container">
    <h1>Complete Your Profile</h1>
    <form action="/submitOwnerInfo" method="POST" enctype="multipart/form-data">
      <label for="petName">Pet's Name:</label>
      <input type="text" id="petName" name="petName" required>

      <label for="petType">Pet's Type:</label>
      <select id="petType" name="petType" required>
        <option value="Cat">Cat</option>
        <option value="Dog">Dog</option>
        <option value="Hamster">Hamster</option>
        <option value="Bird">Bird</option>
      </select>

      <label for="petMedicalHistory">Pet's Medical History:</label>
      <textarea id="petMedicalHistory" name="petMedicalHistory"></textarea>

      <label for="petPhoto">Pet's Photo:</label>
      <input type="file" id="petPhoto" name="petPhoto" accept="image/*">

      <label for="ownerPhoto">Your Photo:</label>
      <input type="file" id="ownerPhoto" name="ownerPhoto" accept="image/*">

      <button type="submit">Submit</button>
    </form>
  </div>
</body>
<img src="/background.jpeg" alt="Description of Image" class="background-image">
</html>

